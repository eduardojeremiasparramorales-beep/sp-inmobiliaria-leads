# =============================================================================
# Caddyfile — SP Inmobiliaria CRM
# Editar con el dominio real (DuckDNS) y recargar: systemctl reload caddy
# =============================================================================

# === PRODUCCIÓN (con HTTPS automático) ===
# Ejemplo: sp-crm.duckdns.org {
#     reverse_proxy localhost:3000
#     encode gzip
# }

# === MODO BACKUP (por IP, sin SSL) ===
:80 {
    reverse_proxy localhost:3000
}
